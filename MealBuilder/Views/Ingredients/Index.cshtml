@model IEnumerable<MealBuilder.Models.Ingredient>

@{
    ViewData["Title"] = "Ingredients";
}

<section class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="mb-1">Ingredients</h1>
            <p class="text-muted mb-0">
                Manage the list of available ingredients.
            </p>
        </div>
        <a asp-action="Create" class="btn btn-primary">
            + Create New
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            There are no ingredients yet. Click <strong>Create New</strong> to add one.
        </div>
    }
    else
    {
        <div class="table-responsive shadow-sm rounded">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width: 60%;">
                            @Html.DisplayNameFor(m => m.First().Name)
                        </th>
                        <th style="width: 40%;" class="text-end">
                            Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-semibold">
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td class="text-end">
                                <div class="btn-group btn-group-sm" role="group">
                                    <a asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-info">
                                        Details
                                    </a>
                                    <a asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-primary">
                                        Edit
                                    </a>
                                    <a asp-action="Delete"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-danger">
                                        Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>